<!-- <template>
  <md-field md-clearable>
    <label>Cleareable</label>
    <md-input v-model="initial"></md-input>
  </md-field>
</template>

<script setup lang="ts">
import { PropType } from 'vue';

const initial = 'sample text!'
const props = defineProps({
  /**
   * The value of Input - either 'string' or 'number'
   * @example default 'string'
   */
  value: {
    type: String as PropType<'' | 0>,
    default: '',
  },
  /**
   * The fullWidth of Input - either true or false
   * @example default 'string'
   */
  fullWidth: {
    type: Boolean as PropType<true | false>,
    default: false,
  },
  /**
   * The value of Input - either true or false
   * @example default 'string'
   */
  outlined: {
    type: Boolean as PropType<true | false>,
    default: false,
  },
  /**
   * The value of Input - either true or false
   * @example default 'string'
   */
  dense: {
    type: Boolean as PropType<true | false>,
    default: false,
  },
  /**
   * focused - either true or false
   * @example default 'string'
   */
  focused: {
    type: Boolean as PropType<true | false>,
    default: false,
  },
  /**
   * textarea - either true or false
   * @example default 'string'
   */
  textarea: {
    type: Boolean as PropType<true | false>,
    default: false,
  },
  /**
   * useNativeValidation - either true or false
   * @example default 'string'
   */
  useNativeValidation: {
    type: Boolean as PropType<true | false>,
    default: true,
  },
  /**
   * valid - either true or false
   * @example default 'string'
   */
  valid: {
    type: Boolean as PropType<true | false>,
    default: true,
  },
});
</script>

<style lang="scss" scoped>
  .md-field:last-child {
    margin-bottom: 40px;
  }
</style> -->
